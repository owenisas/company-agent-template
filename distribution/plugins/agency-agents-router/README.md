# agency-agents-router

Spec 18.2 / 18.5 / 20.3. Lazy specialist search/load/delegation.

This directory is a placeholder. The generated plugin is installed only
from a reviewed upstream commit recorded in
`manifests/agency-agents.lock.yaml`. Do not clone arbitrary upstream HEAD.

Pin: TODO
License: MIT (preserve upstream notice on vendor).
